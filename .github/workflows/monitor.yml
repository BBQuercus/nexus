name: Monitor Railway

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  dev:
    name: Dev Smoke Check
    runs-on: ubuntu-latest
    if: ${{ vars.DEV_FRONTEND_URL != '' && vars.DEV_BACKEND_URL != '' }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: python scripts/smoke_check.py --frontend-url "${{ vars.DEV_FRONTEND_URL }}" --backend-url "${{ vars.DEV_BACKEND_URL }}"

  production:
    name: Production Smoke Check
    runs-on: ubuntu-latest
    if: ${{ vars.PRODUCTION_FRONTEND_URL != '' && vars.PRODUCTION_BACKEND_URL != '' }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: python scripts/smoke_check.py --frontend-url "${{ vars.PRODUCTION_FRONTEND_URL }}" --backend-url "${{ vars.PRODUCTION_BACKEND_URL }}"
